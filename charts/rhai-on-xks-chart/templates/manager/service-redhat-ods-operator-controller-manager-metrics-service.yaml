{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    name: rhods-operator
  name: rhai-operator-controller-manager-metrics-service
  namespace: {{ .Values.rhaiOperator.namespace }}
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    name: rhai-operator
{{- end }}
